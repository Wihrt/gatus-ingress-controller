{{- if .Values.webhook.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "gatus-ingress-controller.fullname" . }}-webhook-tls
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gatus-ingress-controller.labels" . | nindent 4 }}
spec:
  secretName: {{ include "gatus-ingress-controller.fullname" . }}-webhook-tls
  issuerRef:
    name: {{ include "gatus-ingress-controller.fullname" . }}-selfsigned
    kind: Issuer
  dnsNames:
    - {{ include "gatus-ingress-controller.fullname" . }}-webhook.{{ .Release.Namespace }}.svc
    - {{ include "gatus-ingress-controller.fullname" . }}-webhook.{{ .Release.Namespace }}.svc.cluster.local
{{- end }}
